# Variables de entorno de la lambda
dev:
  NODE_ENV: 'dev'
  apiGateway:
    restApiId: xxxxx # REST API resource ID. Default is generated by the framework
    restApiRootResourceId: xxxxx # Root resource, represent as / path


test:
  NODE_ENV: 'test'
  apiGateway:
    restApiId: xxxxx # REST API resource ID. Default is generated by the framework
    restApiRootResourceId: xxxxx # Root resource, represent as / path

prod:
  NODE_ENV: 'prod'
